{{- if .Values.tests.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-startup-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: {{ .Release.Name }}-startup-test
      image: {{ include "rocketchat.imageRegistry" . }}{{ .Values.tests.image.repository }}:{{ .Values.tests.image.tag }}
      imagePullPolicy: {{ .Values.tests.image.pullPolicy | default .Values.image.pullPolicy | quote }}
      {{- with .Values.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      env:
        - name: ROCKETCHAT_HOST
          value: http://{{ template "rocketchat.fullname" . }}.{{ .Release.Namespace }}
      command:
        - /bin/bash
        - -ec
        - echo "testing host $ROCKETCHAT_HOST" && ./wait_http.sh "$ROCKETCHAT_HOST" && ./basic_test.sh "$ROCKETCHAT_HOST"
  restartPolicy: Never
{{- end }}