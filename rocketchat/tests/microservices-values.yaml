federation:
  enabled: true
  host: "synapse.${ROCKETCHAT_HOST}"

microservices:
  enabled: true
  
nats:
  # not needed for testing.
  # locally exhausting open files for this container. I don't want to troubleshoot this right now.
  # ^ Deb
  reloader:
    enabled: false

host: "${ROCKETCHAT_HOST}"
image:
  tag: "${ROCKETCHAT_TAG}"
ingress:
  enabled: true
  federation:
    serveWellKnown: true
    